#! /usr/bin/env ruby
$LOAD_PATH.unshift File.expand_path(File.join(File.dirname(__FILE__), '/../lib'))
require 'widget'

# Now run command the user specified on the command line
Commandable.execute(ARGV)
